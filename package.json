{
  "scripts": {
    "build": "tsc && tslint src/*",
    "test": "npm run build && jest"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  },
  "devDependencies": {
    "@types/jest": "^24.0.6",
    "@types/node": "^11.9.2",
    "@types/rewire": "^2.5.28",
    "ajv": "^5.0.0",
    "jest": "^24.1.0",
    "rewire": "^4.0.1",
    "ts-jest": "^24.0.0",
    "tslint": "^5.12.1",
    "typescript": "^3.3.3"
  }
}
